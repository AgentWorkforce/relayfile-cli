# relayfile-cli

A standalone CLI substrate for RelayFile-adjacent command execution, retrieval, and artifact capture.

## Purpose

This repo is intended to hold the reusable CLI-facing layer that can:
- run provider CLIs
- capture artifacts/files/results
- support bounded E2E proofs
- evolve independently from the core relayfile server/runtime repo

## Status

Initial boundary definition in progress.
